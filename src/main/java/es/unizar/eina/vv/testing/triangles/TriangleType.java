package es.unizar.eina.vv.testing.triangles;

/**
 * Enum class that represents the different types a triangle can be classified
 * as (equilateral, isosceles, and scalene) based on the length of its sides.
 */
public enum TriangleType {
    EQUILATERAL,
    ISOSCELES,
    SCALENE;
}
